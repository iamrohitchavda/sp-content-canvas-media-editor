# Browser-only Vercel design demo: no upload persistence or export.
VITE_DEMO_MODE=true

# For local Koa rendering, leave VITE_RENDER_FUNCTION_URL unset and run npm run server.
# For the AWS Lambda renderer, set false and use the RenderFunctionUrl SAM output.
# VITE_DEMO_MODE=false
# VITE_RENDER_FUNCTION_URL=https://example.lambda-url.region.on.aws
